#!/usr/bin/env sh

# Shared environment for all shells

# # Git Identity
# export GIT_USER="dimkasta"

# # GitHub token
# export GITHUB_TOKEN_FILE="$HOME/.config/dotfiles/github_token"
# if [ -f "$GITHUB_TOKEN_FILE" ]; then
#   export GITHUB_TOKEN="$(cat "$GITHUB_TOKEN_FILE")"
#   export GH_TOKEN="$GITHUB_TOKEN"
# fi

GIT_CREDS_FILE="$HOME/.config/git/github.credentials"

# Load shared aliases if present
if [ -f "$HOME/.shell_aliases" ]; then
  . "$HOME/.shell_aliases"
fi